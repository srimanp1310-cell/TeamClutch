# Person B (macOS, CPU-only) — harness, tests, analysis.
# Person A additionally installs the CUDA build of torch (cu124) and triton on WSL2;
# see README "Setup" for the platform-specific torch index URL.
torch
numpy
pandas
matplotlib
pytest
