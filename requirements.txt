# CPU-only install (macOS/Linux) — harness, tests, analysis.
# The GPU machine additionally installs the CUDA build of torch (cu124) and triton;
# see README "Setup" for the platform-specific torch index URL.
torch
numpy
pandas
matplotlib
pytest
