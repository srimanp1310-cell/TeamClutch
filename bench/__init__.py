"""Benchmark entry points.

`bench/torch_transformer_benchmark.py` is the organizers' file and is kept
byte-identical (SHA-256 recorded in docs/INTERFACE.md). Everything else in this
package imports from it; nothing modifies it.
"""
